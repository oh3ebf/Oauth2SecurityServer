export interface Credentials {
  // Customize received credentials here
  access_token: string;
  expires_in: number;
  organization: string;
  refresh_token: string;
  scope: string;
  token_type: string;
}
